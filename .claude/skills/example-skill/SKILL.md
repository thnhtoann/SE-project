---
name: example-skill
description: Template skill — replace with a real auto-invoked workflow for this project (e.g. scaffolding a new module, running the test suite, generating a design doc).
---

# Example skill

This is a placeholder skill folder showing the expected layout (`.claude/skills/<skill-name>/SKILL.md`).

Replace this file with real instructions for a workflow you want Claude to run automatically, such as:
- Scaffolding a new backend endpoint + matching frontend call
- Generating a requirements doc from a template into `docs/requirements/`
- Running and summarizing the test suite
