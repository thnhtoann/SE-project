---
description: Prepare a deployment checklist for the current changes
---

Before deploying, verify:

1. All tests pass (see `.claude/rules/testing.md` for how tests are run).
2. `docs/test/` reflects the latest test plan/results if this is a milestone release.
3. No secrets or credentials are committed.
4. Summarize what's changing since the last deploy.

This project has no deploy pipeline configured yet — update this command once one exists.
