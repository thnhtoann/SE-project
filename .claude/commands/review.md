---
description: Review the current diff against project conventions
---

Review the pending changes (`git diff`) for:

1. Adherence to `.claude/rules/code-style.md` and `.claude/rules/api-conventions.md`
2. Missing or weak tests per `.claude/rules/testing.md`
3. Obvious bugs, edge cases, or security issues

Report findings grouped by severity (blocking / suggestion / nit). Do not modify files unless asked.
